python-telegram-bot==20.7
groq==0.4.2
google-generativeai==0.4.1
openai==1.12.0
chromadb==0.4.22
numpy==1.26.4
posthog<4
python-dotenv==1.0.1
aiohttp==3.9.3
requests==2.31.0
beautifulsoup4==4.12.3
dnspython==2.6.1
APScheduler==3.10.4
aiosqlite==0.20.0
lxml==5.1.0
rank-bm25==0.2.2
fpdf2==2.7.9
gTTS==2.5.1
matplotlib==3.8.4
flask==3.0.3

# Dev / testing
pytest==8.2.0

# Optional: cross-encoder reranker for sharper recall (heavy, pulls torch).
# Install only if you want it; retrieval works without it (RRF fusion).
# sentence-transformers==2.7.0

# Optional integrations (safe to skip; imported lazily):
# Google Calendar
google-api-python-client==2.122.0
google-auth==2.29.0
google-auth-oauthlib==1.2.0
# X / Twitter
tweepy==4.14.0

# Perception (all optional; imported lazily, bot runs without them):
# Headless browser automation
# playwright==1.43.0        # then: python -m playwright install chromium
# Local voice transcription (needs ffmpeg available)
# faster-whisper==1.0.1
# Document parsing (PDF / DOCX)
# pypdf==4.2.0
# python-docx==1.1.0
